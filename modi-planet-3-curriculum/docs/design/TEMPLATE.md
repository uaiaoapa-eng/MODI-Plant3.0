# [기능명] 설계문서

> 작성: <AI 모델> + <작성자> | 날짜: YYYY-MM-DD | 상태: Draft / Approved / Implemented
> 규칙: 이 문서를 쓰기 **전에** 관련 코드를 실제로 읽고 검증할 것. 상상으로 쓴 앵커는 구현을 실패시킨다.

## 1. 배경과 목표

<!-- 왜 하는가. 1~2문단. 어떤 문제가 있고, 이 변경 후 무엇이 달라지는가. -->

## 2. 현재 상태 (검증됨)

<!-- 설계 전에 실제 코드를 읽고 확인한 사실만 적는다. 각 항목에 파일:라인 근거를 붙인다. -->

| 확인한 사실 | 근거 (파일:라인) |
|---|---|
| 예: 토큰 발급은 `issue_token()`에서 처리 | `src/api/auth.py:28` |
| 예: 세션 모델에 refresh 필드 없음 | `src/models/session.py:15-40` |

## 3. 설계

### 3.1 변경 개요

<!-- 무엇을 어떻게 바꾸는지. 다이어그램이 필요하면 mermaid. -->

### 3.2 인터페이스 계약

<!-- 새로 생기거나 바뀌는 시그니처/스키마를 코드로. 산문 금지. -->

```
(실제 시그니처 / 요청·응답 스키마)
```

### 3.3 데이터 변경

<!-- 마이그레이션, 스키마 변경, 기존 데이터 처리. 없으면 "없음"이라고 명시. -->

## 4. 하지 않는 것 (Non-goals)

<!-- 이번 설계 범위 밖. 구현자가 "겸사겸사" 건드리기 쉬운 것을 미리 차단. -->

- 

## 5. 엣지 케이스와 결정 사항

<!-- 설계 단계에서 이미 내린 결정. 구현자가 임의 판단할 여지를 없앤다. -->

| 상황 | 결정 |
|---|---|
| | |

## 6. 구현 이슈 분해

<!-- 각 이슈는 세션 1개에서 끝나는 크기. 의존 순서 명시. 각 이슈는 ai-task 템플릿으로 등록.
     병렬 안전 규칙: 의존 없는 이슈끼리는 코드 앵커가 겹치면 안 된다 — 겹치면 의존을 넣거나 분해를 다시. -->

| # | 이슈 제목 | 의존 | 검증 명령어 |
|---|---|---|---|
| 1 | | 없음 | |
| 2 | | #1 | |

## 7. 전체 완료 기준

<!-- 모든 이슈가 끝났을 때 최종적으로 확인할 것. 실행 가능한 명령어로. -->

- [ ] 
