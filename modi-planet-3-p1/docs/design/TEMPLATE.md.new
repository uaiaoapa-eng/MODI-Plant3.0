# [기능명] 설계문서

> 작성: <AI 모델> + <작성자> | 날짜: YYYY-MM-DD | 상태: Draft / Approved / Implemented
> 규칙: 이 문서를 쓰기 **전에** 관련 코드를 실제로 읽고 검증할 것. 상상으로 쓴 앵커는 구현을 실패시킨다.

## 1. 배경과 목표

<!-- 왜 하는가. 1~2문단. 어떤 문제가 있고, 이 변경 후 무엇이 달라지는가. -->

## 2. 현재 상태 (검증됨)

<!-- 설계 전에 실제 코드를 읽고 확인한 사실만 적는다. 각 항목에 파일:라인 근거를 붙인다. -->

| 확인한 사실 | 근거 (파일:라인) |
|---|---|
| 예: 토큰 발급은 `issue_token()`에서 처리 | `src/api/auth.py:28` |
| 예: 세션 모델에 refresh 필드 없음 | `src/models/session.py:15-40` |

## 3. 설계

### 3.1 변경 개요

<!-- 무엇을 어떻게 바꾸는지. 다이어그램이 필요하면 mermaid. -->

### 3.2 인터페이스 계약

<!-- 새로 생기거나 바뀌는 시그니처/스키마를 코드로. 산문 금지. -->

```
(실제 시그니처 / 요청·응답 스키마)
```

### 3.3 데이터 변경

<!-- 마이그레이션, 스키마 변경, 기존 데이터 처리. 없으면 "없음"이라고 명시. -->

### 3.4 UI 시안

<!-- /spec 1단계 UI 판정의 확정 기록. 판정도 근거 필수 — '없음'도 기록한다 ("판단 안 함"과 구분).
     모양 판정: 사람 눈에 보이는 표면의 모양·구성이 바뀌는가.
     의미 판정: 모양은 그대로인데 표시되는 값의 의미·계산이 바뀌는가 (픽셀 불변이어도 UI 결정). -->

- 모양 판정: 있음 / 없음 — 근거: <표면 접점 파일:라인 목록, '없음'이면 "탐색한 경로: ...">
- 의미 판정: 있음 / 없음
- 티어: full / light / 없음
- (full) 승인 시안: `.design/<슬러그>/vN.html` · fidelity: full/partial/fresh · 승인일: YYYY-MM-DD · 수정 라운드: N회
- (full) 시안 계약 수준: 구조·상태·문구만 / 픽셀 수준
- 범위 밖으로 분리한 시안 요구: <없으면 "없음">

<!-- (full) 승인 시안을 이산 체크리스트로 증류 — 구현 이슈의 (수동) 완료 기준과 리뷰가 이 항목 수준에서 판정한다. -->

| 구분 (keep/new/changed) | 요소 | 상태 (빈 상태 포함) | 문구 |
|---|---|---|---|
| | | | |

<!-- (의미 판정 '있음') 표시 값 의미 변경 표 — AS-IS 의미는 실제 코드에서 확인한 사실만. -->

| 값 | 표시 위치 (파일:라인) | AS-IS 의미 | TO-BE 의미 |
|---|---|---|---|
| | | | |

> 머지 이후에는 코드가 진실이고, 시안은 승인 당시의 합의 이력이다.

## 4. 하지 않는 것 (Non-goals)

<!-- 이번 설계 범위 밖. 구현자가 "겸사겸사" 건드리기 쉬운 것을 미리 차단. -->

- 

## 5. 엣지 케이스와 결정 사항

<!-- 설계 단계에서 이미 내린 결정. 구현자가 임의 판단할 여지를 없앤다. -->

| 상황 | 결정 |
|---|---|
| | |

## 6. 구현 이슈 분해

<!-- 각 이슈는 세션 1개에서 끝나는 크기. 의존 순서 명시. 각 이슈는 ai-task 템플릿으로 등록.
     병렬 안전 규칙: 의존 없는 이슈끼리는 코드 앵커가 겹치면 안 된다 — 겹치면 의존을 넣거나 분해를 다시.
     UI 열: full(시안 필수) / light(출력 검증) / 없음 — 시안 링크와 (수동) 시안 대조 항목은 full 이슈에만 붙는다. -->

| # | 이슈 제목 | 의존 | UI | 검증 명령어 |
|---|---|---|---|---|
| 1 | | 없음 | 없음 | |
| 2 | | #1 | full | |

## 7. 전체 완료 기준

<!-- 모든 이슈가 끝났을 때 최종적으로 확인할 것. 실행 가능한 명령어로. -->

- [ ] 
